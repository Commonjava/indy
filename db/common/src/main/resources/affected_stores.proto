package affected_stores;

message StoreKeySet
{
    repeated StoreKey storeKeys = 1;
}

message StoreKey
{
    optional string packageType = 1;

    enum StoreType {
        group = 0;
        remote = 1;
        hosted = 2;
    }

    optional StoreType type = 2;

    optional string name = 3;
}